import { ClaseConDecorador } from "./main";

const instancia1 = new ClaseConDecorador();
const instancia2 = new ClaseConDecorador();

console.log(`${JSON.stringify(instancia1)} ⚠️`);
console.log(`${JSON.stringify(instancia2)} ⚠️`);

// Decorador simple ejecutado ⚠️
// {} ⚠️
// {} ⚠️
