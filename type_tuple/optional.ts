const tupla: [string, number, boolean] = ["1", 1, true];

//console.log(`${tupla[3]} ⚠️`)

const tuplaOpcional: [string, number, boolean?] = ["0", 0];

const [ceroCaracter, ceroNumero] = tuplaOpcional;
